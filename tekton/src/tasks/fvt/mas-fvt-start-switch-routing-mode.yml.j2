---
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: mas-fvt-start-switch-routing-mode
spec:
  params:
    {{ lookup('template', task_src_dir ~ '/common/cli-params.yml.j2') | indent(4) }}
    - name: mas_instance_id
      type: string
      description: Instance ID
    - name: mas_routing_mode
      type: string
      description: Target routing mode (subdomain or path)

  steps:
    - name: start-switch-routing-mode
      script: |
        #!/usr/bin/env bash
        echo "Switching routing mode to: $MAS_ROUTING_MODE"
        
        # Update the Suite CR with the new routing mode
        oc patch suite $MAS_INSTANCE_ID -n mas-$MAS_INSTANCE_ID-core --type=merge -p "{\"spec\":{\"settings\":{\"routingMode\":\"$MAS_ROUTING_MODE\"}}}"
        
        RESULT=$?
        echo 'Routing mode switch initiated'
        echo '--------------------------------------------------------------------------------'
        echo "Target routing mode: $MAS_ROUTING_MODE"
        echo '--------------------------------------------------------------------------------'
        exit $RESULT
      timeout: "0"
      image: quay.io/ibmmas/cli:latest
      imagePullPolicy: $(params.image_pull_policy)
      env:
        {{ lookup('template', task_src_dir ~ '/common/cli-env.yml.j2') | indent(8) }}
        - name: MAS_INSTANCE_ID
          value: $(params.mas_instance_id)
        - name: MAS_ROUTING_MODE
          value: $(params.mas_routing_mode)