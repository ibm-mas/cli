timeout: "2h"
taskRef:
  kind: Task
  name: mas-fvt-run-suite
when:
  - input: "$(params.fvt_digest_aiservice)"
    operator: notin
    values: [""]
  - input: "$(params.aiservice_channel)"
    operator: notin
    values: [""]
workspaces:
  - name: configs
    workspace: shared-configs