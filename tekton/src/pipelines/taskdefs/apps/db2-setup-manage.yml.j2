- name: suite-db2-setup-manage
  params:
    {{ lookup('template', 'taskdefs/common/cli-params.yml.j2') | indent(4) }}
    - name: devops_suite_name
      value: suite-db2-setup-manage

    - name: db2_instance_name
      value: "mas-$(params.mas_instance_id)-$(params.mas_workspace_id)-manage"
  # Only perform the hack if Manage is being installed and if we're are using a dedicated Db2 instance for Manage
  when:
    - input: "$(params.mas_app_channel_manage)"
      operator: notin
      values: [""]
    - input: "$(params.db2_action_manage)"
      operator: in
      values: ["install"]
    - input: "$(params.configure_external_db)"
      operator: notin
      values: ["true"]  
  taskRef:
    name: mas-devops-suite-db2-setup-for-manage
    kind: Task