---
- hosts: localhost
  any_errors_fatal: true
  vars:
    # Image Pull Policy
    image_pull_policy: "{{ lookup('env', 'IMAGE_PULL_POLICY') }}"
    # AI Service Details
    aiservice_instance_id: "{{ lookup('env', 'AISERVICE_INSTANCE_ID') }}"
    aiservice_channel: "{{ lookup('env', 'AISERVICE_CHANNEL') }}"
    aiservice_model_versions: "{{ lookup('env', 'AISERVICE_MODEL_VERSIONS') }}"

    # FVT Configuration
    fvt_image_registry: "{{ lookup('env', 'FVT_IMAGE_REGISTRY') }}"
    fvt_artifactory_username: "{{ lookup('env', 'FVT_ARTIFACTORY_USERNAME') }}"
    fvt_artifactory_token: "{{ lookup('env', 'FVT_ARTIFACTORY_TOKEN') }}"
    fvt_digest_aiservice: "{{ lookup('env', 'FVT_DIGEST_AISERVICE') }}"
    ivt_digest_core: "{{ lookup('env', 'IVT_DIGEST_CORE') }}"
    # Pipeline Run Info
    devops_build_number: "{{ lookup('env', 'DEVOPS_BUILD_NUMBER') | default('0', True) }}"
    pipelinerun_name: "{{ lookup('env', 'PIPELINERUN_NAME') | default('aiservice-fvt', True) }}-{{ devops_build_number }}"
    pipelinerun_namespace: "{{ lookup('env', 'PIPELINERUN_NAMESPACE') | default('aiservice-' ~ aiservice_instance_id ~ '-pipelines', True) }}"
  tasks:
    - name: "Debug"
      debug:
        msg:
          - "pipelinerun_name .................. {{ pipelinerun_name }}"
          - "pipelinerun_namespace ............. {{ pipelinerun_namespace }}"
          - ""
          - "aiservice_channel ................. {{ aiservice_channel }}"
          - "aiservice_instance_id ............. {{ aiservice_instance_id }}"
          - ""
          - "fvt_image_registry ................ {{ fvt_image_registry }}"
          - "fvt_artifactory_username .......... {{ fvt_artifactory_username }}"
          - "fvt_artifactory_token ............. {{ fvt_artifactory_token }}"
          - "fvt_digest_aiservice .............. {{ fvt_digest_aiservice }}"
          - "ivt_digest_core ................... {{ ivt_digest_core }}"
          - "aiservice_model_versions .......... {{ aiservice_model_versions }}"

    - name: "Start fvt-aiservice pipeline"
      kubernetes.core.k8s:
        apply: true
        template: templates/aiservice-fvt.yml.j2
