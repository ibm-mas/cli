#!/usr/bin/env bash

function gitops_iac_help() {
  [[ -n "$1" ]] && echo_warning "$1"
  reset_colors
  cat << EOM


Usage:
  mas gitops-iac-efs [options]
Where ${COLOR_YELLOW}specified${TEXT_RESET} each option may also be defined by setting the appropriate environment variable.
When no options are specified on the command line, interactive-mode will be enabled by default.

Basic Configuration:
  -a, --account-id ${COLOR_YELLOW}ACCOUNT_ID${TEXT_RESET}                    Account name that the cluster belongs to
  -c, --cluster-id ${COLOR_YELLOW}CLUSTER_ID${TEXT_RESET}                    Cluster ID
  -m, --mas-instance-id ${COLOR_YELLOW}MAS_INSTANCE_ID${TEXT_RESET}          MAS Instance ID
  -r, --region-id ${COLOR_YELLOW}REGION_ID${TEXT_RESET}                      Region ID
  --region-name ${COLOR_YELLOW}REGION_NAME${TEXT_RESET}                      Region Name


Secrets Manager:
      --secrets-path ${COLOR_YELLOW}SECRETS_PATH${TEXT_RESET}                Secrets Manager path
      --secrets-key-seperator ${COLOR_YELLOW}SECRETS_KEY_SEPERATOR${TEXT_RESET}


Target Cluster (Optional):
      --cluster-url ${COLOR_YELLOW}CLUSTER_URL${TEXT_RESET}                  Set to target a remote Kubernetes cluster (defaults to 'https://kubernetes.default.svc')

Automatic GitHub Push (Optional):
  -P, --github-push ${COLOR_YELLOW}GITHUB_PUSH${TEXT_RESET}                  Enable automatic push to GitHub
  -H, --github-host ${COLOR_YELLOW}GITHUB_HOST${TEXT_RESET}                  GitHub Hostname for your GitOps repository
  -O, --github-org ${COLOR_YELLOW}GITHUB_ORG${TEXT_RESET}                    Github org for your GitOps repository
  -R, --github-repo ${COLOR_YELLOW}GITHUB_REPO${TEXT_RESET}                  Github repo for your GitOps repository
  -S, --github-ssh ${COLOR_YELLOW}GIT_SSH${TEXT_RESET}                       Git ssh key path
  -B, --git-branch ${COLOR_YELLOW}GIT_BRANCH${TEXT_RESET}                    Git branch to commit to of your GitOps repository
  -M, --git-commit-msg ${COLOR_YELLOW}GIT_COMMIT_MSG${TEXT_RESET}            Git commit message to use when committing to of your GitOps repository

Other Commands:
  -h, --help                                      Show this help message
EOM
  [[ -n "$1" ]] && exit 1 || exit 0
}

export GIT_COMMIT_MSG=${GIT_COMMIT_MSG:-"iac provision"}

function gitops_iac_noninteractive() {
  GITOPS_WORKING_DIR=$PWD/working-dir
  SECRETS_KEY_SEPERATOR="/"

  # Set default values
  export CLUSTER_URL=${CLUSTER_URL:-"https://kubernetes.default.svc"}
  
  while [[ $# -gt 0 ]]
  do
    key="$1"
    shift
    case $key in
      # GitOps Configuration
      -d|--dir)
        export GITOPS_WORKING_DIR=$1 && shift
        ;;
      -a|--account-id)
        export ACCOUNT_ID=$1 && shift
        ;;
      -c|--cluster-id)
        export CLUSTER_ID=$1 && shift
        export CLUSTER_NAME=$1  # For backward compatibility
        ;;
      
      -m|--mas-instance-id)
        export MAS_INSTANCE_ID=$1 && shift
        ;;
      
      -r|--region-id)
        export REGION_ID=$1 && shift
        ;;

      # AWS Secrets Manager Configuration
      --sm-aws-secret-region)
        export SM_AWS_REGION=$1
        shift
        ;;
     --region-name)
        export REGION_NAME=$1 && shift
        ;;
      --sm-aws-access-key)
        export SM_AWS_ACCESS_KEY_ID=$1 && shift
        ;;
      --sm-aws-secret-key)
        export SM_AWS_SECRET_ACCESS_KEY=$1 && shift
        ;;
      --secrets-path)
        export SECRETS_PATH=$1 && shift
        ;;

      # EFS Configuration
      --is_efs)
        export IS_EFS=$1 && shift
        ;;
      --efs-source)
        export EFS_SOURCE=$1 && shift
        ;;
      --efs_manage_db2_performance_mode)
        export EFS_MANAGE_DB2_PERFORMANCE_MODE=$1 && shift
        ;;

      --efs_manage_db2_mount_count)
        export EFS_MANAGE_DB2_MOUNT_COUNT=$1 && shift
        ;;

      --efs_manage_db2_throughput_mode)
        export EFS_MANAGE_DB2_THROUGHPUT_MODE=$1 && shift
        ;;

      --efs_manage_main_performance_mode)
        export EFS_MANAGE_MAIN_PERFORMANCE_MODE=$1 && shift
        ;;

      --efs_manage_main_mount_count)
        export EFS_MANAGE_MAIN_MOUNT_COUNT=$1 && shift
        ;;

      --efs_manage_main_throughput_mode)
        export EFS_MANAGE_MAIN_THROUGHPUT_MODE=$1 && shift
        ;;

      --efs_iot_db2_performance_mode)
        export EFS_IOT_DB2_PERFORMANCE_MODE=$1 && shift
        ;;

      --efs_iot_db2_mount_count)
        export EFS_IOT_DB2_MOUNT_COUNT=$1 && shift
        ;;

      --efs_iot_db2_throughput_mode)
        export EFS_IOT_DB2_THROUGHPUT_MODE=$1 && shift
        ;;

      --efs_facilities_db2_performance_mode)
        export EFS_FACILITIES_DB2_PERFORMANCE_MODE=$1 && shift
        ;;

      --efs_facilities_db2_mount_count)
        export EFS_FACILITIES_DB2_MOUNT_COUNT=$1 && shift
        ;;

      --efs_facilities_db2_throughput_mode)
        export EFS_FACILITIES_DB2_THROUGHPUT_MODE=$1 && shift
        ;;

      --efs_facilities_main_performance_mode)
        export EFS_FACILITIES_MAIN_PERFORMANCE_MODE=$1 && shift
        ;;

      --efs_facilities_main_mount_count)
        export EFS_FACILITIES_MAIN_MOUNT_COUNT=$1 && shift
        ;;

      --efs_facilities_main_throughput_mode)
        export EFS_FACILITIES_MAIN_THROUGHPUT_MODE=$1 && shift
        ;;

      --efs_visualinspection_main_performance_mode)
        export EFS_VISUALINSPECTION_MAIN_PERFORMANCE_MODE=$1 && shift
        ;;

      --efs_visualinspection_main_mount_count)
        export EFS_VISUALINSPECTION_MAIN_MOUNT_COUNT=$1 && shift
        ;;

      --efs_visualinspection_main_throughput_mode)
        export EFS_VISUALINSPECTION_MAIN_THROUGHPUT_MODE=$1 && shift
        ;;

      # S3 Configuration
      --is_s3)
        export IS_S3=$1 && shift
        ;;
      --s3_source)
        export S3_SOURCE=$1 && shift
        ;;
      --s3_source_iampolicy)
        export S3_SOURCE_IAMPOLICY=$1 && shift
        ;;

      --s3_source_s3accesspoint)
        export S3_SOURCE_S3ACCESSPOINT=$1 && shift
        ;;
      --s3_manage)
        export S3_MANAGE=$1 && shift
        ;;
      --s3_mvi)
        export S3_MVI=$1 && shift
        ;;

      # Target Cluster (Optional)
      --cluster-url)
        export CLUSTER_URL=$1 && shift
        ;;

      # Automatic GitHub Push
      -P|--github-push)
        export GITHUB_PUSH=true
        ;;
      -H|--github-host)
        export GITHUB_HOST=$1 && shift
        ;;
      -O|--github-org)
        export GITHUB_ORG=$1 && shift
        ;;
      -R|--github-repo)
        export GITHUB_REPO=$1 && shift
        ;;
      -S|--github-ssh)
        export GIT_SSH=$1 && shift
        ;;
      -B|--git-branch)
        export GIT_BRANCH=$1 && shift
        ;;
      -M|--git-commit-msg)
        export GIT_COMMIT_MSG=$1 && shift
        ;;

      # Other Commands
      -h|--help)
        gitops_iac_help
        ;;
      *)
        # unknown option
        echo -e "${COLOR_RED}Usage Error: Unsupported option \"${key}\"${COLOR_RESET}\n"
        gitops_iac_help "Usage Error: Unsupported option \"${key}\" "
        exit 1
        ;;
      esac
  done

  # Validate required parameters
  [[ -z "$GITOPS_WORKING_DIR" ]] && gitops_iac_help "GITOPS_WORKING_DIR is not set"
  [[ -z "$ACCOUNT_ID" ]] && gitops_iac_help "ACCOUNT_ID is not set"
  [[ -z "$CLUSTER_ID" ]] && gitops_iac_help "CLUSTER_ID is not set"
  [[ -z "$MAS_INSTANCE_ID" ]] && gitops_iac_help "MAS_INSTANCE_ID is not set"
  [[ -z "$REGION_ID" && -z "$SM_AWS_REGION" ]] && gitops_iac_help "REGION_ID or SM_AWS_REGION is not set"
  [[ -z "$REGION_NAME" ]] && gitops_iac_help "REGION_NAME is not set"
  [[ -z "$CLUSTER_URL" ]] && gitops_iac_help "CLUSTER_URL is not set"

  if [[ "$GITHUB_PUSH" == "true" ]]; then
    [[ -z "$GITHUB_HOST" ]] && gitops_iac_help "GITHUB_HOST is not set"
    [[ -z "$GITHUB_ORG" ]] && gitops_iac_help "GITHUB_ORG is not set"
    [[ -z "$GITHUB_REPO" ]] && gitops_iac_help "GITHUB_REPO is not set"
    [[ -z "$GIT_BRANCH" ]] && gitops_iac_help "GIT_BRANCH is not set"
  fi
}

# ============================================================================
# Function 1: Create New Branch from Target Branch
# ============================================================================
# Clones the target branch (GIT_BRANCH) and creates a NEW working branch from it
# The new branch will be used for all commits and will be merged back to GIT_BRANCH via PR
#
# Parameters: Uses environment variables set by gitops_iac_noninteractive
# Exports: NEW_BRANCH_NAME (saved to file for other tasks)
function gitops_iac_create_branch() {
  echo
  echo_h2 "Creating New Working Branch from ${GIT_BRANCH}"
  
  # Validate required parameters
  [[ -z "$GITOPS_WORKING_DIR" ]] && gitops_iac_help "GITOPS_WORKING_DIR is not set"
  [[ -z "$GITHUB_HOST" ]] && gitops_iac_help "GITHUB_HOST is not set"
  [[ -z "$GITHUB_ORG" ]] && gitops_iac_help "GITHUB_ORG is not set"
  [[ -z "$GITHUB_REPO" ]] && gitops_iac_help "GITHUB_REPO is not set"
  [[ -z "$GIT_BRANCH" ]] && gitops_iac_help "GIT_BRANCH is not set (target branch)"
  [[ -z "$ACCOUNT_ID" ]] && gitops_iac_help "ACCOUNT_ID is not set"
  [[ -z "$CLUSTER_ID" ]] && gitops_iac_help "CLUSTER_ID is not set"
  [[ -z "$MAS_INSTANCE_ID" ]] && gitops_iac_help "MAS_INSTANCE_ID is not set"
  
  mkdir -p ${GITOPS_WORKING_DIR}
  
  if [ -z $GIT_SSH ]; then
    export GIT_SSH=false
  fi
  
  # Clone target branch (e.g., main, develop, etc.)
  echo
  echo "GITOPS_WORKING_DIR  ${GITOPS_WORKING_DIR}"
  echo_h2 "Cloning GitHub repo $GITHUB_ORG/$GITHUB_REPO from branch ${GIT_BRANCH}"
  clone_target_git_repo $GITHUB_HOST $GITHUB_ORG $GITHUB_REPO $GIT_BRANCH $GITOPS_WORKING_DIR $GIT_SSH
  
  # Create new working branch with timestamp
  TIMESTAMP=$(date +%Y%m%d-%H%M%S)
  NEW_BRANCH="gitops-iac-${ACCOUNT_ID}-${CLUSTER_ID}-${MAS_INSTANCE_ID}-${TIMESTAMP}"
  
  echo
  echo_h2 "Creating new working branch: ${NEW_BRANCH}"
  cd ${GITOPS_WORKING_DIR}/${GITHUB_REPO}
  git checkout -b ${NEW_BRANCH} || exit 1
  git push -u origin ${NEW_BRANCH} || exit 1
  cd -
  
  # Save the new branch name for other tasks to use
  echo "${NEW_BRANCH}" > ${GITOPS_WORKING_DIR}/new_branch_name.txt
  echo "${GIT_BRANCH}" > ${GITOPS_WORKING_DIR}/target_branch_name.txt
  
  echo
  echo_h2 "✅ Working Branch Created Successfully"
  echo "Target Branch (for PR): ${GIT_BRANCH}"
  echo "New Working Branch: ${NEW_BRANCH}"
  echo "Branch names saved to: ${GITOPS_WORKING_DIR}/"
  
  exit 0
}

# ============================================================================
# Function 2: Provision EFS Resources
# ============================================================================
# Generates EFS-specific Terraform files and commits to the working branch
#
# Parameters: Uses environment variables set by gitops_iac_noninteractive
# Requires: Branch name from gitops_iac_create_branch
function gitops_iac_provision_efs() {
  echo
  echo_h2 "Provisioning EFS Resources"
  
  # Validate required parameters
  [[ -z "$GITOPS_WORKING_DIR" ]] && gitops_iac_help "GITOPS_WORKING_DIR is not set"
  [[ -z "$GITHUB_REPO" ]] && gitops_iac_help "GITHUB_REPO is not set"
  [[ -z "$REGION_NAME" ]] && gitops_iac_help "REGION_NAME is not set"
  [[ -z "$MAS_INSTANCE_ID" ]] && gitops_iac_help "MAS_INSTANCE_ID is not set"
  [[ -z "$CLUSTER_ID" ]] && gitops_iac_help "CLUSTER_ID is not set"
  
  # Read the working branch name created by previous task
  if [ -f "${GITOPS_WORKING_DIR}/new_branch_name.txt" ]; then
    WORKING_BRANCH=$(cat ${GITOPS_WORKING_DIR}/new_branch_name.txt)
    echo "Using working branch: ${WORKING_BRANCH}"
  else
    echo "ERROR: Branch name file not found. Run gitops_iac_create_branch first."
    exit 1
  fi
  
  GITOPS_CLUSTER_DIR=${GITOPS_WORKING_DIR}/${GITHUB_REPO}/${REGION_NAME}/instances/${MAS_INSTANCE_ID}
  mkdir -p ${GITOPS_CLUSTER_DIR}
  
  echo
  echo_h2 "Generating IAC/EFS Configuration"
  
  #Creating file path for extracting parameter value from cluster & vpc
  CLUSTER_CIDR_WORKLOAD_CLUSTER_PATH=${GITOPS_WORKING_DIR}/${GITHUB_REPO}/${REGION_NAME}/clusters/${CLUSTER_ID}
  MAIN_FILE_PATH=$CLUSTER_CIDR_WORKLOAD_CLUSTER_PATH/main.tf
  TERRAFORMTF_FILE_PATH=$CLUSTER_CIDR_WORKLOAD_CLUSTER_PATH/terraform.tfvars
  
  #Creating value path for extracting parameter value from .tf file
  VPC_ID_DOT_PATH="vpc_id"
  VPC_NAME_DOT_PATH="vpc_name"
  CLUSTER_CIDR_WORKLOAD_EXTERNAL_DOT_PATH="locals.cluster.cidrs.workload_external"
  VPC_CIDR_WORKLOAD_INTERNAL_DOT_PATH="locals.cluster.cidrs.workload_internal"
  EFS_CIDR_REF_NAME=${CLUSTER_ID}-${MAS_INSTANCE_ID}-efs
  VPC_CIDR_WORKLOAD_DOT_PATH="locals.workloads[\"${EFS_CIDR_REF_NAME}\"].cidrs"
  
  #Extracting values from .tf file
  export CLUSTER_CIDR_WORKLOAD_EXTERNAL=$(get_tf_value "${MAIN_FILE_PATH}" "${CLUSTER_CIDR_WORKLOAD_EXTERNAL_DOT_PATH}")
  export CLUSTER_CIDR_WORKLOAD_INTERNAL=$(get_tf_value "${MAIN_FILE_PATH}" "${VPC_CIDR_WORKLOAD_INTERNAL_DOT_PATH}")
  export VPC_ID_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${VPC_ID_DOT_PATH}")
  export VPC_NAME_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${VPC_NAME_DOT_PATH}")
  VPC_CIDR_PATH=${GITOPS_WORKING_DIR}/${GITHUB_REPO}/${REGION_NAME}/vpcs/${VPC_NAME_VALUE}/main.tf
  export VPC_CIDR_WORKLOAD_INTERNAL=$(get_tf_value "${VPC_CIDR_PATH}" "${VPC_CIDR_WORKLOAD_DOT_PATH}")
  
  echo "CLUSTER_CIDR_WORKLOAD_EXTERNAL  ${CLUSTER_CIDR_WORKLOAD_EXTERNAL}"
  echo "CLUSTER_CIDR_WORKLOAD_INTERNAL  ${CLUSTER_CIDR_WORKLOAD_INTERNAL}"
  echo "VPC_CIDR_WORKLOAD_INTERNAL  ${VPC_CIDR_WORKLOAD_INTERNAL}"
  
  # Check if EFS parameters are set and export flags for template usage
  if [[ -n "$EFS_MANAGE_DB2_PERFORMANCE_MODE" && -n "$EFS_MANAGE_DB2_MOUNT_COUNT" && -n "$EFS_MANAGE_DB2_THROUGHPUT_MODE" ]]; then
    export EFS_MANAGE=true
    echo "EFS_MANAGE set to true"
  else
    export EFS_MANAGE=false
  fi
  
  if [[ -n "$EFS_IOT_DB2_PERFORMANCE_MODE" && -n "$EFS_IOT_DB2_MOUNT_COUNT" && -n "$EFS_IOT_DB2_THROUGHPUT_MODE" ]]; then
    export EFS_IOT=true
    echo "EFS_IOT set to true"
  else
    export EFS_IOT=false
  fi
  
  if [[ -n "$EFS_FACILITIES_DB2_PERFORMANCE_MODE" && -n "$EFS_FACILITIES_DB2_MOUNT_COUNT" && -n "$EFS_FACILITIES_DB2_THROUGHPUT_MODE" ]]; then
    export EFS_FACILITIES=true
    echo "EFS_FACILITIES set to true"
  else
    export EFS_FACILITIES=false
  fi
  
  if [[ -n "$EFS_VISUALINSPECTION_MAIN_PERFORMANCE_MODE" && -n "$EFS_VISUALINSPECTION_MAIN_MOUNT_COUNT" && -n "$EFS_VISUALINSPECTION_MAIN_THROUGHPUT_MODE" ]]; then
    export EFS_VISUALINSPECTION=true
    echo "EFS_VISUALINSPECTION set to true"
  else
    export EFS_VISUALINSPECTION=false
  fi
  
  # Save EFS variables for common files generation
  echo "${CLUSTER_CIDR_WORKLOAD_EXTERNAL}" > ${GITOPS_WORKING_DIR}/efs_vars_CLUSTER_CIDR_WORKLOAD_EXTERNAL.txt
  echo "${CLUSTER_CIDR_WORKLOAD_INTERNAL}" > ${GITOPS_WORKING_DIR}/efs_vars_CLUSTER_CIDR_WORKLOAD_INTERNAL.txt
  echo "${VPC_CIDR_WORKLOAD_INTERNAL}" > ${GITOPS_WORKING_DIR}/efs_vars_VPC_CIDR_WORKLOAD_INTERNAL.txt
  echo "${VPC_ID_VALUE}" > ${GITOPS_WORKING_DIR}/efs_vars_VPC_ID_VALUE.txt
  echo "${VPC_NAME_VALUE}" > ${GITOPS_WORKING_DIR}/efs_vars_VPC_NAME_VALUE.txt
  echo "${EFS_MANAGE}" > ${GITOPS_WORKING_DIR}/efs_vars_EFS_MANAGE.txt
  echo "${EFS_IOT}" > ${GITOPS_WORKING_DIR}/efs_vars_EFS_IOT.txt
  echo "${EFS_FACILITIES}" > ${GITOPS_WORKING_DIR}/efs_vars_EFS_FACILITIES.txt
  echo "${EFS_VISUALINSPECTION}" > ${GITOPS_WORKING_DIR}/efs_vars_EFS_VISUALINSPECTION.txt
  
  # Generate EFS-specific file only
  echo "Generating IAC EFS file ${GITOPS_CLUSTER_DIR}/efs.tf"
  jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-efs.yaml.j2 ${GITOPS_CLUSTER_DIR}/efs.tf
  
  # Commit changes to working branch
  if [ "$GITHUB_PUSH" == "true" ]; then
    echo
    echo_h2 "Committing EFS changes to branch ${WORKING_BRANCH}"
    GIT_COMMIT_MSG="Add EFS configuration"
    save_to_target_git_repo "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$WORKING_BRANCH" "${GITOPS_WORKING_DIR}/${GITHUB_REPO}" "${GIT_COMMIT_MSG}"
  fi
  
  echo
  echo_h2 "✅ EFS Resources Provisioned and Committed"
  
  exit 0
}

# ============================================================================
# Function 3: Provision S3 Resources
# ============================================================================
# Generates S3-specific Terraform files and commits to the working branch
#
# Parameters: Uses environment variables set by gitops_iac_noninteractive
# Requires: Branch name from gitops_iac_create_branch
function gitops_iac_provision_s3() {
  echo
  echo_h2 "Provisioning S3 Resources"
  
  # Validate required parameters
  [[ -z "$GITOPS_WORKING_DIR" ]] && gitops_iac_help "GITOPS_WORKING_DIR is not set"
  [[ -z "$GITHUB_REPO" ]] && gitops_iac_help "GITHUB_REPO is not set"
  [[ -z "$REGION_NAME" ]] && gitops_iac_help "REGION_NAME is not set"
  [[ -z "$MAS_INSTANCE_ID" ]] && gitops_iac_help "MAS_INSTANCE_ID is not set"
  [[ -z "$CLUSTER_ID" ]] && gitops_iac_help "CLUSTER_ID is not set"
  
  # Read the working branch name created by previous task
  if [ -f "${GITOPS_WORKING_DIR}/new_branch_name.txt" ]; then
    WORKING_BRANCH=$(cat ${GITOPS_WORKING_DIR}/new_branch_name.txt)
    echo "Using working branch: ${WORKING_BRANCH}"
  else
    echo "ERROR: Branch name file not found. Run gitops_iac_create_branch first."
    exit 1
  fi
  
  GITOPS_CLUSTER_DIR=${GITOPS_WORKING_DIR}/${GITHUB_REPO}/${REGION_NAME}/instances/${MAS_INSTANCE_ID}
  mkdir -p ${GITOPS_CLUSTER_DIR}
  
  echo
  echo_h2 "Generating IAC/S3 Configuration"
  
  #Creating file path for extracting parameter value from cluster
  CLUSTER_CIDR_WORKLOAD_CLUSTER_PATH=${GITOPS_WORKING_DIR}/${GITHUB_REPO}/${REGION_NAME}/clusters/${CLUSTER_ID}
  TERRAFORMTF_FILE_PATH=$CLUSTER_CIDR_WORKLOAD_CLUSTER_PATH/terraform.tfvars
  
  #Creating value path for extracting parameter value from .tf file
  NAME_PREFIX="name_prefix"
  COST_CENTER_DOT_PATH='common_tags["cost-center"]'
  CONTACT_DOT_PATH='common_tags["contact"]'
  ENVIRONMENT_DOT_PATH='common_tags["environment"]'
  OWNER_DOT_PATH='common_tags["owner"]'
  ACCOUNT_ID_DOT_PATH='account_id'
  
  #Extracting values from .tf file
  export NAME_PREFIX_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${NAME_PREFIX}")
  export COST_CENTER_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${COST_CENTER_DOT_PATH}")
  export CONTACT_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${CONTACT_DOT_PATH}")
  export ENVIRONMENT_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${ENVIRONMENT_DOT_PATH}")
  export OWNER_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${OWNER_DOT_PATH}")
  export ACCOUNT_ID=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${ACCOUNT_ID_DOT_PATH}")
  
  echo "NAME_PREFIX_VALUE  ${NAME_PREFIX_VALUE}"
  echo "COST_CENTER_VALUE  ${COST_CENTER_VALUE}"
  echo "CONTACT_VALUE      ${CONTACT_VALUE}"
  echo "ENVIRONMENT_VALUE  ${ENVIRONMENT_VALUE}"
  echo "ACCOUNT_ID         ${ACCOUNT_ID}"
  
  # Save S3 variables for common files generation
  echo "${NAME_PREFIX_VALUE}" > ${GITOPS_WORKING_DIR}/s3_vars_NAME_PREFIX_VALUE.txt
  echo "${COST_CENTER_VALUE}" > ${GITOPS_WORKING_DIR}/s3_vars_COST_CENTER_VALUE.txt
  echo "${CONTACT_VALUE}" > ${GITOPS_WORKING_DIR}/s3_vars_CONTACT_VALUE.txt
  echo "${ENVIRONMENT_VALUE}" > ${GITOPS_WORKING_DIR}/s3_vars_ENVIRONMENT_VALUE.txt
  echo "${OWNER_VALUE}" > ${GITOPS_WORKING_DIR}/s3_vars_OWNER_VALUE.txt
  echo "${ACCOUNT_ID}" > ${GITOPS_WORKING_DIR}/s3_vars_ACCOUNT_ID.txt
  
  # Generate S3-specific files only
  if [[ "$S3_MANAGE"  == "true" ]]; then
    echo "Generating s3-instance-manage.tf file ${GITOPS_CLUSTER_DIR}/s3-instance-manage.tf "
    jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-s3instance-manage.yaml.j2 ${GITOPS_CLUSTER_DIR}/s3-instance-manage.tf
    
    echo "Generating ibm-iac-iampolicy-manage.tf file ${GITOPS_CLUSTER_DIR}/ibm-iac-iampolicy-manage.tf "
    jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-iampolicy-manage.yaml.j2 ${GITOPS_CLUSTER_DIR}/iampolicy-manage.tf
    
    echo "Generating s3-accesspoints-manage.tf file ${GITOPS_CLUSTER_DIR}/s3-accesspoints-manage.tf"
    jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-s3accesspoints-manage.yaml.j2 ${GITOPS_CLUSTER_DIR}/s3-accesspoints-manage.tf
  fi
  
  # Commit changes to working branch
  if [ "$GITHUB_PUSH" == "true" ]; then
    echo
    echo_h2 "Committing S3 changes to branch ${WORKING_BRANCH}"
    GIT_COMMIT_MSG="Add S3 configuration"
    save_to_target_git_repo "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$WORKING_BRANCH" "${GITOPS_WORKING_DIR}/${GITHUB_REPO}" "${GIT_COMMIT_MSG}"
  fi
  
  echo
  echo_h2 "✅ S3 Resources Provisioned and Committed"
  
  exit 0
}

# ============================================================================
# Function 4: Generate Common IAC Files
# ============================================================================
# Generates common Terraform files (main.tf, variables.tf, terraform.tfvars)
# This should be called AFTER EFS and S3 provisioning to use their exported variables
#
# Parameters: Uses environment variables set by gitops_iac_noninteractive
# Requires: Branch name and exported variables from previous tasks
function gitops_iac_generate_common_files() {
  echo
  echo_h2 "Generating Common IAC Files"
  
  # Validate required parameters
  [[ -z "$GITOPS_WORKING_DIR" ]] && gitops_iac_help "GITOPS_WORKING_DIR is not set"
  [[ -z "$GITHUB_REPO" ]] && gitops_iac_help "GITHUB_REPO is not set"
  [[ -z "$REGION_NAME" ]] && gitops_iac_help "REGION_NAME is not set"
  [[ -z "$MAS_INSTANCE_ID" ]] && gitops_iac_help "MAS_INSTANCE_ID is not set"
  
  # Read the working branch name created by first task
  if [ -f "${GITOPS_WORKING_DIR}/new_branch_name.txt" ]; then
    WORKING_BRANCH=$(cat ${GITOPS_WORKING_DIR}/new_branch_name.txt)
    echo "Using working branch: ${WORKING_BRANCH}"
  else
    echo "ERROR: Branch name file not found. Run gitops_iac_create_branch first."
    exit 1
  fi
  
  GITOPS_CLUSTER_DIR=${GITOPS_WORKING_DIR}/${GITHUB_REPO}/${REGION_NAME}/instances/${MAS_INSTANCE_ID}
  mkdir -p ${GITOPS_CLUSTER_DIR}
  
  # Load exported variables from EFS task (if exists)
  if [ -f "${GITOPS_WORKING_DIR}/efs_vars_CLUSTER_CIDR_WORKLOAD_EXTERNAL.txt" ]; then
    export CLUSTER_CIDR_WORKLOAD_EXTERNAL=$(cat ${GITOPS_WORKING_DIR}/efs_vars_CLUSTER_CIDR_WORKLOAD_EXTERNAL.txt)
    export CLUSTER_CIDR_WORKLOAD_INTERNAL=$(cat ${GITOPS_WORKING_DIR}/efs_vars_CLUSTER_CIDR_WORKLOAD_INTERNAL.txt)
    export VPC_CIDR_WORKLOAD_INTERNAL=$(cat ${GITOPS_WORKING_DIR}/efs_vars_VPC_CIDR_WORKLOAD_INTERNAL.txt)
    export VPC_ID_VALUE=$(cat ${GITOPS_WORKING_DIR}/efs_vars_VPC_ID_VALUE.txt)
    export VPC_NAME_VALUE=$(cat ${GITOPS_WORKING_DIR}/efs_vars_VPC_NAME_VALUE.txt)
    export EFS_MANAGE=$(cat ${GITOPS_WORKING_DIR}/efs_vars_EFS_MANAGE.txt)
    export EFS_IOT=$(cat ${GITOPS_WORKING_DIR}/efs_vars_EFS_IOT.txt)
    export EFS_FACILITIES=$(cat ${GITOPS_WORKING_DIR}/efs_vars_EFS_FACILITIES.txt)
    export EFS_VISUALINSPECTION=$(cat ${GITOPS_WORKING_DIR}/efs_vars_EFS_VISUALINSPECTION.txt)
    echo "Loaded EFS variables"
  fi
  
  # Load exported variables from S3 task (if exists)
  if [ -f "${GITOPS_WORKING_DIR}/s3_vars_NAME_PREFIX_VALUE.txt" ]; then
    export NAME_PREFIX_VALUE=$(cat ${GITOPS_WORKING_DIR}/s3_vars_NAME_PREFIX_VALUE.txt)
    export COST_CENTER_VALUE=$(cat ${GITOPS_WORKING_DIR}/s3_vars_COST_CENTER_VALUE.txt)
    export CONTACT_VALUE=$(cat ${GITOPS_WORKING_DIR}/s3_vars_CONTACT_VALUE.txt)
    export ENVIRONMENT_VALUE=$(cat ${GITOPS_WORKING_DIR}/s3_vars_ENVIRONMENT_VALUE.txt)
    export OWNER_VALUE=$(cat ${GITOPS_WORKING_DIR}/s3_vars_OWNER_VALUE.txt)
    export ACCOUNT_ID=$(cat ${GITOPS_WORKING_DIR}/s3_vars_ACCOUNT_ID.txt)
    echo "Loaded S3 variables"
  fi
  
  echo "Checking and creating common files"
  create_common_files ${GITOPS_CLUSTER_DIR}
  
  # Generate common terraform files using variables exported by EFS/S3 tasks
  echo "Generating main.tf file ${GITOPS_CLUSTER_DIR}/main.tf "
  jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-mainTF.yaml.j2 ${GITOPS_CLUSTER_DIR}/main.tf
  
  echo "Generating terraform.tfvars file ${GITOPS_CLUSTER_DIR}/terraform.tfvars"
  jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-terraformTFvars.yaml.j2 ${GITOPS_CLUSTER_DIR}/terraform.tfvars
  
  echo "Generating variables.tf file ${GITOPS_CLUSTER_DIR}/variables.tf"
  jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-variablesTF.yaml.j2 ${GITOPS_CLUSTER_DIR}/variables.tf
  
  # Commit changes to working branch
  if [ "$GITHUB_PUSH" == "true" ]; then
    echo
    echo_h2 "Committing common files to branch ${WORKING_BRANCH}"
    GIT_COMMIT_MSG="Add common Terraform configuration files"
    save_to_target_git_repo "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$WORKING_BRANCH" "${GITOPS_WORKING_DIR}/${GITHUB_REPO}" "${GIT_COMMIT_MSG}"
  fi
  
  echo
  echo_h2 "✅ Common IAC Files Generated and Committed"
  echo "Directory: ${GITOPS_CLUSTER_DIR}"
  
  exit 0
}

# ============================================================================
# Function 5: Manage Pull Request Lifecycle
# ============================================================================
# Creates PR from working branch to target branch, monitors, approves, applies, and merges
#
# Parameters: Uses environment variables set by gitops_iac_noninteractive
# Requires: Branch names from gitops_iac_create_branch
function gitops_iac_manage_pr() {
  echo
  echo_h2 "Managing Pull Request Lifecycle"
  
  # Validate required parameters
  [[ -z "$GITHUB_HOST" ]] && gitops_iac_help "GITHUB_HOST is not set"
  [[ -z "$GITHUB_ORG" ]] && gitops_iac_help "GITHUB_ORG is not set"
  [[ -z "$GITHUB_REPO" ]] && gitops_iac_help "GITHUB_REPO is not set"
  [[ -z "$GITOPS_WORKING_DIR" ]] && gitops_iac_help "GITOPS_WORKING_DIR is not set"
  
  # Read the working branch and target branch names
  if [ -f "${GITOPS_WORKING_DIR}/new_branch_name.txt" ]; then
    WORKING_BRANCH=$(cat ${GITOPS_WORKING_DIR}/new_branch_name.txt)
    TARGET_BRANCH=$(cat ${GITOPS_WORKING_DIR}/target_branch_name.txt)
    echo "Working Branch: ${WORKING_BRANCH}"
    echo "Target Branch (for PR): ${TARGET_BRANCH}"
  else
    echo "ERROR: Branch name files not found. Run gitops_iac_create_branch first."
    exit 1
  fi
  
  GIT_COMMIT_MSG="IAC provision: EFS and S3 configuration"
  
  echo
  echo_h2 "Creating Pull Request from ${WORKING_BRANCH} to ${TARGET_BRANCH}"
  
  # Create PR from working branch to target branch
  PR_NUMBER=$(git_commit_and_create_pr_if_none_exist \
    "$GITHUB_HOST" \
    "$GITHUB_ORG" \
    "$GITHUB_REPO" \
    "$TARGET_BRANCH" \
    "${GITOPS_WORKING_DIR}/${GITHUB_REPO}" \
    "${GIT_COMMIT_MSG}" \
    "gitops-iac-update")
  
  echo "Created PR #${PR_NUMBER}"
  
  # Clean up
  remove_git_repo_clone $GITOPS_WORKING_DIR/$GITHUB_REPO
  
  # Monitor PR status if PR was created
  if [[ -n "${PR_NUMBER}" && "${PR_NUMBER}" != "null" ]]; then
    
    echo_h2 "Monitoring PR #${PR_NUMBER} Status"
    ATLANTIS_UNLOCK_COMMENT_BODY="atlantis unlock"
    PR_PLAN_COMMENT=$(read_and_validate_pr_comments "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER")
    echo "PR_PLAN_COMMENT: "$PR_PLAN_COMMENT
    
    if [[ "$PR_PLAN_COMMENT" == "Passed" ]]; then
      echo "Going to approve PR: "${PR_NUMBER}
      APPROVED_PR=$(approve_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$IAC_DEV_GITHUB_PAT")
      echo "PR approved ${APPROVED_PR}"
      if [[ "$APPROVED_PR" == "true" ]]; then
        sleep 120
        COMMENT_BODY="atlantis apply"
        ATLANTIS_APPLY_COMMENT=$(comment_on_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$COMMENT_BODY")
        if [[ "$ATLANTIS_APPLY_COMMENT" == "true" ]]; then
          
          PR_APPLY_COMMENT=$(read_and_validate_pr_comments "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "apply")
          
          if [[ "$PR_APPLY_COMMENT" == "Passed" ]]; then
            MERGE_PR_STATUS=$(merge_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "Merge PR")
            if [[ "$MERGE_PR_STATUS" != "true" ]]; then
              echo_h2 "⚠️  Atlantis apply succeeded. Failed to merge PR. Please check the PR: 'https://${GITHUB_HOST}/${GITHUB_ORG}/${GITHUB_REPO}/pull/${PR_NUMBER}'"
              exit 1
            fi
          else
            echo_h2 "⚠️  Atlantis Apply Failed. Pipeline Failed."
            comment_on_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$ATLANTIS_UNLOCK_COMMENT_BODY"
            close_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER"
            exit 1
          fi
        else
          echo_h2 "⚠️  Failed to add 'Atlantis Apply' on PR. PIPELINE FAILED. CLOSING PR."
          comment_on_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$ATLANTIS_UNLOCK_COMMENT_BODY"
          close_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER"
          exit 1
        fi
      else
        echo_h2 "⚠️  Failed to approve PR. Closing PR. Pipeline Failed."
        comment_on_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$ATLANTIS_UNLOCK_COMMENT_BODY"
        close_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER"
        exit 1
      fi
    else
      echo_h2 "⚠️  Atlantis Plan Failed. Closing PR. Pipeline Failed."
      comment_on_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$ATLANTIS_UNLOCK_COMMENT_BODY"
      close_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER"
      exit 1
    fi
  else
    echo
    echo_h2 "⚠️  No PR number returned, pipeline failed."
    exit 1
  fi
  
  echo
  echo_h2 "✅ Pull Request Lifecycle Complete - Merged to ${TARGET_BRANCH}"
  
  exit 0
}

# ============================================================================
# Command Wrappers - Make functions callable as separate commands
# ============================================================================

# Wrapper for gitops-iac-create-branch command
function gitops_iac_create_branch_cmd() {
  shift  # Remove command name
  if [[ $# -gt 0 ]]; then
    gitops_iac_noninteractive "$@"
  else
    echo "Not supported yet"
    exit 1
  fi
  
  # Call the actual function
  gitops_iac_create_branch
}

# Wrapper for gitops-iac-provision-efs command
function gitops_iac_provision_efs_cmd() {
  shift  # Remove command name
  if [[ $# -gt 0 ]]; then
    gitops_iac_noninteractive "$@"
  else
    echo "Not supported yet"
    exit 1
  fi
  
  # Call the actual function
  gitops_iac_provision_efs
}

# Wrapper for gitops-iac-provision-s3 command
function gitops_iac_provision_s3_cmd() {
  shift  # Remove command name
  if [[ $# -gt 0 ]]; then
    gitops_iac_noninteractive "$@"
  else
    echo "Not supported yet"
    exit 1
  fi
  
  # Call the actual function
  gitops_iac_provision_s3
}

# Wrapper for gitops-iac-generate-common-files command
function gitops_iac_generate_common_files_cmd() {
  shift  # Remove command name
  if [[ $# -gt 0 ]]; then
    gitops_iac_noninteractive "$@"
  else
    echo "Not supported yet"
    exit 1
  fi
  
  # Call the actual function
  gitops_iac_generate_common_files
}

# Wrapper for gitops-iac-manage-pr command
function gitops_iac_manage_pr_cmd() {
  shift  # Remove command name
  if [[ $# -gt 0 ]]; then
    gitops_iac_noninteractive "$@"
  else
    echo "Not supported yet"
    exit 1
  fi
  
  # Call the actual function
  gitops_iac_manage_pr
}


function gitops_iac() {
  # Take the first parameter off (it will be create-gitops)
  shift
  if [[ $# -gt 0 ]]; then
    gitops_iac_noninteractive "$@"
  else
    echo "Not supported yet"
    exit 1
    gitops_iac_efs_interactive
  fi

  # catch errors
  set -o pipefail
  trap 'echo "[ERROR] Error occurred at $BASH_SOURCE, line $LINENO, exited with $?"; exit 1' ERR

  mkdir -p ${GITOPS_WORKING_DIR}
  GITOPS_CLUSTER_DIR=${GITOPS_WORKING_DIR}/${GITHUB_REPO}/${REGION_NAME}/instances/${MAS_INSTANCE_ID}

  echo
  reset_colors
  echo_h2 "Review Settings"

  echo "${TEXT_DIM}"
  echo_h2 "Target" "    "
  echo_reset_dim "Account ID ..................... ${COLOR_MAGENTA}${ACCOUNT_ID}"
  echo_reset_dim "Region Name .................... ${COLOR_MAGENTA}${REGION_NAME}"
  echo_reset_dim "Cluster ID ..................... ${COLOR_MAGENTA}${CLUSTER_ID}"
  echo_reset_dim "MAS Instance ID ................ ${COLOR_MAGENTA}${MAS_INSTANCE_ID}"
  echo_reset_dim "Cluster URL .................... ${COLOR_MAGENTA}${CLUSTER_URL}"
  echo_reset_dim "Cluster Config Directory ....... ${COLOR_MAGENTA}${GITOPS_CLUSTER_DIR}"
  reset_colors

  echo "${TEXT_DIM}"
  if [[ "$GITHUB_PUSH" == "true" ]]; then
    echo_h2 "GitOps Target" "    "
    echo_reset_dim "Automatic Push ................. ${COLOR_GREEN}Enabled"
    echo_reset_dim "Working Directory .............. ${COLOR_MAGENTA}${GITOPS_WORKING_DIR}"
    echo_reset_dim "Host ........................... ${COLOR_MAGENTA}${GITHUB_HOST}"
    echo_reset_dim "Organization ................... ${COLOR_MAGENTA}${GITHUB_ORG}"
    echo_reset_dim "Repository ..................... ${COLOR_MAGENTA}${GITHUB_REPO}"
    echo_reset_dim "Branch ......................... ${COLOR_MAGENTA}${GIT_BRANCH}"
  else
    echo_h2 "GitOps Target" "    "
    echo_reset_dim "Automatic Push ................. ${COLOR_RED}Disabled"
    echo_reset_dim "Working Directory .............. ${COLOR_MAGENTA}${GITOPS_WORKING_DIR}"
  fi
  reset_colors
   echo_reset_dim "IS EFS  ......................... ${COLOR_MAGENTA}${IS_EFS}"

  if [ -z $GIT_SSH ]; then
    export GIT_SSH=false
  fi

  # Clone github target repo
  # ---------------------------------------------------------------------------
  if [ "$GITHUB_PUSH" == "true" ]; then
    echo
    echo "GITOPS_WORKING_DIR  ${GITOPS_WORKING_DIR}"
    echo "GITOPS_CLUSTER_DIR  ${GITOPS_CLUSTER_DIR}"
    echo_h2 "Cloning GitHub repo $GITHUB_ORG $GITHUB_REPO"
    clone_target_git_repo $GITHUB_HOST $GITHUB_ORG $GITHUB_REPO $GIT_BRANCH $GITOPS_WORKING_DIR $GIT_SSH
  fi
  mkdir -p ${GITOPS_CLUSTER_DIR}


  # ---------------------------------------------------------------------------
  echo "Checking and creating common files if not already present"
  create_common_files ${GITOPS_CLUSTER_DIR}


  # ---------------------------------------------------------------------------
  #  creating files for efs.tf only
  if [[ "$IS_EFS"  == "true" ]]; then
    echo
    echo_h2 "Generating IAC/EFS Configuration"
    echo "- IAC Base Config"

    echo "generating path for CLUSTER_CIDR_WORKLOAD_EXTERNAL"

    #Creating file path for extracting parameter value from cluster & vpc
    CLUSTER_CIDR_WORKLOAD_CLUSTER_PATH=${GITOPS_WORKING_DIR}/${GITHUB_REPO}/${REGION_NAME}/clusters/${CLUSTER_ID}
    MAIN_FILE_PATH=$CLUSTER_CIDR_WORKLOAD_CLUSTER_PATH/main.tf
    TERRAFORMTF_FILE_PATH=$CLUSTER_CIDR_WORKLOAD_CLUSTER_PATH/terraform.tfvars
    # VPC_CIDR_PATH=${GITOPS_WORKING_DIR}/${GITHUB_REPO}/${REGION_NAME}/vpcs/${VPC_NAME_VALUE}/main.tf

    #Creating value path for extracting parameter value from .tf file
    VPC_ID_DOT_PATH="vpc_id"
    VPC_NAME_DOT_PATH="vpc_name"
    CLUSTER_CIDR_WORKLOAD_EXTERNAL_DOT_PATH="locals.cluster.cidrs.workload_external"
    VPC_CIDR_WORKLOAD_INTERNAL_DOT_PATH="locals.cluster.cidrs.workload_internal"
    EFS_CIDR_REF_NAME=${CLUSTER_ID}-${MAS_INSTANCE_ID}-efs
    VPC_CIDR_WORKLOAD_DOT_PATH="locals.workloads[\"${EFS_CIDR_REF_NAME}\"].cidrs"

    #Extracting values from .tf file
    export CLUSTER_CIDR_WORKLOAD_EXTERNAL=$(get_tf_value "${MAIN_FILE_PATH}" "${CLUSTER_CIDR_WORKLOAD_EXTERNAL_DOT_PATH}")
    export CLUSTER_CIDR_WORKLOAD_INTERNAL=$(get_tf_value "${MAIN_FILE_PATH}" "${VPC_CIDR_WORKLOAD_INTERNAL_DOT_PATH}")
    export VPC_ID_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${VPC_ID_DOT_PATH}")
    export VPC_NAME_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${VPC_NAME_DOT_PATH}")
    VPC_CIDR_PATH=${GITOPS_WORKING_DIR}/${GITHUB_REPO}/${REGION_NAME}/vpcs/${VPC_NAME_VALUE}/main.tf
    export VPC_CIDR_WORKLOAD_INTERNAL=$(get_tf_value "${VPC_CIDR_PATH}" "${VPC_CIDR_WORKLOAD_DOT_PATH}")

    echo "CLUSTER_CIDR_WORKLOAD_EXTERNAL  ${CLUSTER_CIDR_WORKLOAD_EXTERNAL}"
    echo "CLUSTER_CIDR_WORKLOAD_INTERNAL  ${CLUSTER_CIDR_WORKLOAD_INTERNAL}"
    echo "VPC_CIDR_WORKLOAD_INTERNAL  ${VPC_CIDR_WORKLOAD_INTERNAL}"

    # Check if EFS parameters are set and export flags for template usage
    # Check for manage (db2 and main)
    if [[ -n "$EFS_MANAGE_DB2_PERFORMANCE_MODE" && -n "$EFS_MANAGE_DB2_MOUNT_COUNT" && -n "$EFS_MANAGE_DB2_THROUGHPUT_MODE" ]]; then
      export EFS_MANAGE=true
      echo "EFS_MANAGE set to true"
    else
      export EFS_MANAGE=false
    fi

    # Check for iot_db2
    if [[ -n "$EFS_IOT_DB2_PERFORMANCE_MODE" && -n "$EFS_IOT_DB2_MOUNT_COUNT" && -n "$EFS_IOT_DB2_THROUGHPUT_MODE" ]]; then
      export EFS_IOT=true
      echo "EFS_IOT set to true"
    else
      export EFS_IOT=false
    fi

    # Check for facilities (db2 and main)
    if [[ -n "$EFS_FACILITIES_DB2_PERFORMANCE_MODE" && -n "$EFS_FACILITIES_DB2_MOUNT_COUNT" && -n "$EFS_FACILITIES_DB2_THROUGHPUT_MODE" ]]; then
      export EFS_FACILITIES=true
      echo "EFS_FACILITIES set to true"
    else
      export EFS_FACILITIES=false
    fi

    # Check for visualinspection_main
    if [[ -n "$EFS_VISUALINSPECTION_MAIN_PERFORMANCE_MODE" && -n "$EFS_VISUALINSPECTION_MAIN_MOUNT_COUNT" && -n "$EFS_VISUALINSPECTION_MAIN_THROUGHPUT_MODE" ]]; then
      export EFS_VISUALINSPECTION=true
      echo "EFS_VISUALINSPECTION set to true"
    else
      export EFS_VISUALINSPECTION=false
    fi

    #Generating .tf files using templates
    echo "Generating main.tf file ${GITOPS_CLUSTER_DIR}/main.tf "
    jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-mainTF.yaml.j2 ${GITOPS_CLUSTER_DIR}/main.tf

    echo "Generating terraform.tfvars file ${GITOPS_CLUSTER_DIR}/terraform.tfvars"
    jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-terraformTFvars.yaml.j2 ${GITOPS_CLUSTER_DIR}/terraform.tfvars

    echo "Generating IAC EFS file ${GITOPS_CLUSTER_DIR}/efs.tf"
    jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-efs.yaml.j2 ${GITOPS_CLUSTER_DIR}/efs.tf

    echo "Generating IAC Variables file ${GITOPS_CLUSTER_DIR}/variables.tf"
    jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-variablesTF.yaml.j2 ${GITOPS_CLUSTER_DIR}/variables.tf

    echo $GIT_COMMIT_MSG
    GIT_COMMIT_MSG="${GIT_COMMIT_MSG}-efs"
  fi

  if [[ "$IS_S3"  == "true" ]]; then


    #Creating file path for extracting parameter value from cluster & vpc
    CLUSTER_CIDR_WORKLOAD_CLUSTER_PATH=${GITOPS_WORKING_DIR}/${GITHUB_REPO}/${REGION_NAME}/clusters/${CLUSTER_ID}
    MAIN_FILE_PATH=$CLUSTER_CIDR_WORKLOAD_CLUSTER_PATH/main.tf
    TERRAFORMTF_FILE_PATH=$CLUSTER_CIDR_WORKLOAD_CLUSTER_PATH/terraform.tfvars

    #Creating value path for extracting parameter value from .tf file
    NAME_PREFIX="name_prefix"
    COST_CENTER_DOT_PATH='common_tags["cost-center"]'
    CONTACT_DOT_PATH='common_tags["contact"]'
    ENVIRONMENT_DOT_PATH='common_tags["environment"]'
    IAM_USER_ARN_DOT_PATH="iam_user_arn_prefix"
    OWNER_DOT_PATH='common_tags["owner"]'
    ACCOUNT_ID_DOT_PATH='account_id'


    #Extracting values from .tf file
    export NAME_PREFIX_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${NAME_PREFIX}")
    export COST_CENTER_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${COST_CENTER_DOT_PATH}")
    export CONTACT_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${CONTACT_DOT_PATH}")
    export ENVIRONMENT_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${ENVIRONMENT_DOT_PATH}")
    export OWNER_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${OWNER_DOT_PATH}")
    export ACCOUNT_ID=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${ACCOUNT_ID_DOT_PATH}")
#    IAM_USER_ARN_VALUE=$(get_tf_value "${TERRAFORMTF_FILE_PATH}" "${IAM_USER_ARN_DOT_PATH}")

    echo "NAME_PREFIX_VALUE  ${NAME_PREFIX_VALUE}"
    echo "COST_CENTER_VALUE  ${COST_CENTER_VALUE}"
    echo "CONTACT_VALUE      ${CONTACT_VALUE}"
    echo "ENVIRONMENT_VALUE  ${ENVIRONMENT_VALUE}"
#    echo "IAM_USER_ARN_VALUE ${IAM_USER_ARN_VALUE}"

    # export ACCOUNT_ID=$(get_field "${IAM_USER_ARN_VALUE}" ":" 5)

    echo "ACCOUNT_ID         ${ACCOUNT_ID}"

    if [[ "$S3_MANAGE"  == "true" ]]; then
    #Generating .tf files using templates
      echo "Generating s3-instance-manage.tf file ${GITOPS_CLUSTER_DIR}/s3-instance-manage.tf "
      jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-s3instance-manage.yaml.j2 ${GITOPS_CLUSTER_DIR}/s3-instance-manage.tf
      #Generating .tf files using templates
      echo "Generating ibm-iac-iampolicy-manage.tf file ${GITOPS_CLUSTER_DIR}/ibm-iac-iampolicy-manage.tf "
      jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-iampolicy-manage.yaml.j2 ${GITOPS_CLUSTER_DIR}/iampolicy-manage.tf

      echo "Generating s3-accesspoints-manage.tf file ${GITOPS_CLUSTER_DIR}/s3-accesspoints-manage.tf"
      jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-s3accesspoints-manage.yaml.j2 ${GITOPS_CLUSTER_DIR}/s3-accesspoints-manage.tf
    fi

    echo "Generating terraform.tfvars file ${GITOPS_CLUSTER_DIR}/terraform.tfvars"
    jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-terraformTFvars.yaml.j2 ${GITOPS_CLUSTER_DIR}/terraform.tfvars

    echo "Generating main.tf file ${GITOPS_CLUSTER_DIR}/main.tf "
    jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-mainTF.yaml.j2 ${GITOPS_CLUSTER_DIR}/main.tf

    echo "Generating variables.tf file ${GITOPS_CLUSTER_DIR}/variables.tf"
    jinjanate_commmon $CLI_DIR/templates/gitops/appset-configs/cluster/instance/ibm-iac-variablesTF.yaml.j2 ${GITOPS_CLUSTER_DIR}/variables.tf

    echo $GIT_COMMIT_MSG
    GIT_COMMIT_MSG="${GIT_COMMIT_MSG}-s3"

  fi


  # Commit and push to github target repo with PR creation
  # ---------------------------------------------------------------------------
  if [ "$GITHUB_PUSH" == "true" ]; then
    echo
    echo_h2 "Creating Pull Request for changes in $GITHUB_ORG/$GITHUB_REPO"
    
    # Use the new function that checks for existing PRs and creates a new one
    # Capture the PR number returned by the function
    PR_NUMBER=$(git_commit_and_create_pr_if_none_exist \
      "$GITHUB_HOST" \
      "$GITHUB_ORG" \
      "$GITHUB_REPO" \
      "$GIT_BRANCH" \
      "${GITOPS_WORKING_DIR}/${GITHUB_REPO}" \
      "${GIT_COMMIT_MSG}")

    echo "Created PR #${PR_NUMBER}"
    # Clean up
    remove_git_repo_clone $GITOPS_WORKING_DIR/$GITHUB_REPO
    
    # Monitor PR status if PR was created
    if [[ -n "${PR_NUMBER}" && "${PR_NUMBER}" != "null" ]]; then
      
      echo_h2 "Monitoring PR #${PR_NUMBER} Status"
      ATLANTIS_UNLOCK_COMMENT_BODY="atlantis unlock"
      PR_PLAN_COMMENT=$(read_and_validate_pr_comments "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER")
      echo "PR_PLAN_COMMENT: "$PR_PLAN_COMMENT
      
      if [[ "$PR_PLAN_COMMENT" == "Passed" ]]; then
        echo "Going to approve PR: "${PR_NUMBER}
        echo "IAC DEV PAT TOKEN: "${IAC_DEV_GITHUB_PAT}
#        APPROVED_PR="false" #temp changes
        APPROVED_PR=$(approve_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$IAC_DEV_GITHUB_PAT")
        echo "PR approved ${APPROVED_PR}"
        if [[ "$APPROVED_PR" == "true" ]]; then
          sleep 120
          COMMENT_BODY="atlantis apply"
          ATLANTIS_APPLY_COMMENT=$(comment_on_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$COMMENT_BODY")
          if [[ "$ATLANTIS_APPLY_COMMENT" == "true" ]]; then

            PR_APPLY_COMMENT=$(read_and_validate_pr_comments "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "apply")

            if [[ "$PR_APPLY_COMMENT" == "Passed" ]]; then
              MARGE_PR_STATUS=$(merge_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "Merge PR")
              if [[ "$MARGE_PR_STATUS" != "true" ]]; then
                echo_h2 "⚠️  Atlantis apply succeeded. Failed to merge PR. Please check the PR: 'https://${GITHUB_HOST}/${GITHUB_ORG}/${GITHUB_REPO}/pull/${PR_NUMBER}'"
                # close_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER"
                exit 1
              fi
            else
              echo_h2 "⚠️  Atlantis Apply Failed. Pipeline Failed."
              comment_on_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$ATLANTIS_UNLOCK_COMMENT_BODY"
              close_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER"
              exit 1
            fi
          else
            echo_h2 "⚠️  Failed to add 'Atlantis Apply' on PR. PIPELINE FAILED. CLOSING PR."
            comment_on_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$ATLANTIS_UNLOCK_COMMENT_BODY"
            close_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER"
            exit 1
          fi
        else
          echo_h2 "⚠️  Failed to approve PR. Closing PR. Pipeline Failed."
          comment_on_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$ATLANTIS_UNLOCK_COMMENT_BODY"
          close_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER"
          exit 1
        fi
      else
        echo_h2 "⚠️  Atlantis Plan Failed. Closing PR. Pipeline Failed."
        comment_on_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER" "$ATLANTIS_UNLOCK_COMMENT_BODY"
        close_pr "$GITHUB_HOST" "$GITHUB_ORG" "$GITHUB_REPO" "$PR_NUMBER"
        exit 1
      fi
    else
      echo
      echo_h2 "⚠️  No PR number returned, pipeline failed."
      exit 1
    fi
  fi

  echo
  echo_h2 "✅ GitOps IAC EFS Configuration Complete"

  exit 0
}


