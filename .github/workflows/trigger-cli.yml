name: workflow_02
on: [repository_dispatch]
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Triggering Workflow 2"
      - name: Checkout
        uses: actions/checkout@v2
