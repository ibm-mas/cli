{
  "terraform": {
    "backend": {
      "s3": {
        "bucket": "{{GITHUB_REPO}}",
        "key": "mas-instance/{{MAS_INSTANCE_ID}}/terraform.tfstate",
        "region": "{{REGION_NAME}}",
        "use_lockfile": true
      }
    }
  }
}