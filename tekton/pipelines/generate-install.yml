- hosts: localhost
  any_errors_fatal: true
  tasks:
    - ansible.builtin.template:
        src: install.yaml.j2
        dest: install.yaml
    - ansible.builtin.template:
        src: install-with-fvt.yaml.j2
        dest: install-with-fvt.yaml
    - ansible.builtin.template:
        src: fvt-assist.yaml.j2
        dest: fvt-assist.yaml
    - ansible.builtin.template:
        src: fvt-core.yaml.j2
        dest: fvt-core.yaml
